<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LauncherApp.Views.PickedItemsPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:LauncherApp.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:BoolToVisibilityConverter x:Key="BoolToVisInverse" Invert="True" />
    </UserControl.Resources>

    <Grid Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="10,8,10,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                x:Name="HeaderText"
                Grid.Column="0"
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="{StaticResource LauncherTextBrush}"
                Text="Picked (0)"
                VerticalAlignment="Center" />

            <Button
                x:Name="ClearAllButton"
                Grid.Column="1"
                Background="Transparent"
                BorderThickness="0"
                Click="ClearAllButton_OnClick"
                Foreground="{StaticResource LauncherSecondaryTextBrush}"
                Padding="6,2"
                FontSize="11"
                Content="Clear all" />
        </Grid>

        <ScrollViewer Grid.Row="1" Padding="6,0,6,8" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PickedItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,6" Margin="0,0,0,4" Background="#1A2433">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Width="20"
                                Height="20"
                                Margin="0,0,8,0"
                                VerticalAlignment="Center">
                                <Grid>
                                    <Image
                                        Width="20"
                                        Height="20"
                                        Source="{Binding Icon}"
                                        Visibility="{Binding HasIcon, Converter={StaticResource BoolToVis}}" />
                                    <TextBlock
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="14"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{StaticResource LauncherMutedTextBrush}"
                                        Text="{Binding IconGlyph}"
                                        Visibility="{Binding HasIcon, Converter={StaticResource BoolToVisInverse}}" />
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
                                <TextBlock
                                    FontSize="12"
                                    FontWeight="Medium"
                                    Foreground="{StaticResource LauncherTextBrush}"
                                    Text="{Binding Title}"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    FontSize="10"
                                    Foreground="{StaticResource LauncherMutedTextBrush}"
                                    Text="{Binding FullPath}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>

                            <Button
                                Grid.Column="2"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="6,2"
                                VerticalAlignment="Center"
                                Tag="{Binding PickedKey}"
                                Click="RemoveButton_OnClick">
                                <TextBlock
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="12"
                                    Foreground="{StaticResource LauncherMutedTextBrush}"
                                    Text="&#xE894;" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
