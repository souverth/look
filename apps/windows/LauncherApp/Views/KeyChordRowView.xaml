<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LauncherApp.Views.KeyChordRowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" ColumnSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="KeyColumn" Width="110" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            x:Name="KeyPill"
            Background="{StaticResource LauncherPanelAltBrush}"
            CornerRadius="10"
            Padding="7,3"
            HorizontalAlignment="Left"
            VerticalAlignment="Center">
            <TextBlock
                x:Name="KeyTextPill"
                FontSize="12"
                Foreground="{StaticResource LauncherTextBrush}" />
        </Border>

        <TextBlock
            x:Name="KeyTextPlain"
            Visibility="Collapsed"
            Foreground="{StaticResource LauncherTextBrush}"
            VerticalAlignment="Center" />

        <TextBlock
            x:Name="DescriptionText"
            Grid.Column="1"
            FontSize="12"
            Foreground="{StaticResource LauncherMutedTextBrush}"
            TextWrapping="Wrap"
            VerticalAlignment="Center" />
    </Grid>
</UserControl>
