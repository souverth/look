# Windows (WinUI3) - Archive Notice

This WinUI3 app is being replaced by the Tauri-based `linows` app at `apps/linows/`.

The new app targets both Windows and Linux with a unified vanilla HTML/CSS/JS frontend,
using the macOS SwiftUI app as the design source of truth.

This directory will be archived once the Tauri migration is complete and
feature parity is confirmed.

Do not add new features here. Bug fixes only until migration is done.
