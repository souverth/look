# Pre-commit hooks configuration
# Hooks auto-installed via nix develop / flake

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ["--maxkb=5000"]
      - id: check-merge-conflict
      - id: check-toml
      - id: debug-statements
      - id: mixed-line-ending
        args: [--fix=lf]

  - repo: local
    hooks:
      - id: cargo-fmt-core
        name: cargo fmt (core workspace, auto-fix)
        entry: cargo fmt --manifest-path core/Cargo.toml --all
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-fmt-ffi
        name: cargo fmt (ffi workspace, auto-fix)
        entry: cargo fmt --manifest-path bridge/ffi/Cargo.toml --all
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-check-core
        name: cargo check (core workspace)
        entry: cargo check --workspace --manifest-path core/Cargo.toml
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-clippy-core
        name: cargo clippy (core workspace)
        entry: cargo clippy --workspace --manifest-path core/Cargo.toml -- -D warnings
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-test-core
        name: cargo test (core workspace)
        entry: cargo test --workspace --manifest-path core/Cargo.toml
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-test-ffi
        name: cargo test (ffi)
        entry: cargo test --manifest-path bridge/ffi/Cargo.toml
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-fmt-linows
        name: cargo fmt (linows/tauri, auto-fix)
        entry: cargo fmt --manifest-path apps/linows/src-tauri/Cargo.toml --all
        language: system
        pass_filenames: false
        types: [rust]

      - id: cargo-clippy-linows
        name: cargo clippy (linows/tauri)
        entry: bash -c 'command -v pkg-config >/dev/null && pkg-config --exists glib-2.0 2>/dev/null && cargo clippy --manifest-path apps/linows/src-tauri/Cargo.toml -- -D warnings || echo "Skipping linows clippy (run inside nix develop)"'
        language: system
        pass_filenames: false
        types: [rust]

      - id: prettier-linows
        name: prettier (linows js/html/css, auto-fix)
        entry: bash -c 'command -v prettier >/dev/null && prettier --write "$@" || echo "Skipping prettier (run inside nix develop)"' --
        language: system
        files: ^apps/linows/src/.*\.(js|html|css)$

      - id: secrets
        name: Detect secrets
        entry: bash -c 'grep -rnE "(password|secret|token|api_key|apikey|private_key)" --include="*.rs" --include="*.toml" --include="*.yaml" --include="*.yml" --exclude-dir=target . | grep -v "^[^\:]+\:\s*//\|#" || true'
        language: system
        pass_filenames: false

ci:
  autoupdate_schedule: monthly
  autoupdate_commit_msg: "chore: update pre-commit hooks"
