[package]
name = "look-todo"
edition = "2024"
license = "MIT"
version = "0.1.0"

[dependencies]
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
